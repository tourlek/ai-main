# Luna task title prefix

- Every new implementation task delegated to Luna must be titled `[Luna Working] - {name}`.
- Apply the prefix when creating the task, or rename the task immediately after creation if the creation flow cannot set its title.
